# 📊 REPORTS MODULE — BRD Clarification Questions

## 📋 SECTION 1 – Reports Grid & Navigation

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|
| 1.1 | Reports Grid | What columns appear in the Reports grid by default? | BRD mentions grid headers with sort/edit-columns option and filter-with-apply-button pattern across all admin grids. | Confirm exact default columns: Report Name, Type, Created By, Last Run, Schedule? Other? | Without the column list we cannot build the grid schema or the frontend table component. | I think this is a good start.<br><br>**Reports:**<br>- Category (e.g., Users, Transactions, Statistics, Alerts, Reservations, IT, etc.)<br>- Scope Level (Location, Pool, Area, User Tier, User Group, User, etc.)<br>- Chart Type<br>- Report Date Range (Previous Quarter, Previous Month, Manual)<br>- Created Date<br>- Modified Date<br>- Owner (System is the default for default reports included)<br>- Last Run Date<br>- Run Status (Scheduled, On-Demand Only, Error Status)<br><br>**Report Schedules:**<br>- Report Name (from above)<br>- Output Type(s)<br>- Report Name Format*<br>- Destination (email address(es), folder path, ???)<br>- Scheduled Interval<br>- Owner<br>- Last Run Date<br>- Run Status<br>- Report Parameters should inherit from the Report itself<br><br>*Allows use of placeholders:<br>- {LOC} -[report name]-[date run in yyyy-mm-dd].[pdf/csv/xlsx]<br>- {OWN} -[report name] - [date run in yyyy-mm-dd].[pdf/csv/xlsx]<br><br>Objective here is to view reports (option to export/deliver/save results) and create templates of the data parameters. Report Schedule sets the automation options for delivery (no view option?) whereas running from the list of Reports displays the view with manual options to export/save |
| 1.2 | Reports Grid | Does the Reports list include a 'Search Quick / AI' bar — and what does it search across? | BRD explicitly labels 'Search QuickAI' on the Reports sub-menu alongside View All, Create, Modify, Sort, Run Report. | Confirm which fields the quick-search runs against: report name only, or also creator, tags, description? | Determines search index and API query scope. | I would run against text fields, with the option to sort/filter results by any column including dates |
| 1.3 | Reports Grid | What does 'Sort' action on the Reports list sort by, and is it user-configurable? | BRD lists 'Sort' as an action option on the Reports sub-menu. | Confirm sort fields and whether users can set a persistent sort preference. | Needed to implement sort dropdown and preference persistence logic. | There would be a grid list of reports with semi-user configurable options. The level of preference persistence can be discussed (e.g., local cookie vs. db level preference) |
| 1.4 | Reports Grid | Can a report be run directly from the grid row, or only after opening it? | BRD lists 'Run Report' as a top-level action alongside View All, Sort. | Confirm: is 'Run Report' a one-click action on the grid row, or does it open a details/config page first? | Affects UX flow and API trigger design. | A report with stored parameters should be able to execute on demand, but . What are the ramifications of not running it with a single click? The Report Schedules section would be responsible for "testing" the delivery method |

## 🔍 SECTION 2 – Report Filters (Generation Parameters)

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|
| 2.1 | Report Filters | When 'Location' is selected as a filter, is the Location filter automatically disabled and replaced when an Area or Pool is selected? | BRD explicitly states: 'Organization bound - disable and auto select if an area or pool is selected; resource types would still allow filtering by location.' | Confirm the exact disable/auto-select cascade logic and whether a warning is shown to the user. | Core filter-dependency logic; must be coded precisely to avoid double-counting. | Double selection avoidance is a matter of ensuring that when selecting by Pool the underlying associated Areas are auto-selected and that de-selecting an Area associated with a Pool requires deselection of the Pool first. In the event that an Area is assigned to multiple pools (not expected to be a common occurrence)...<br><br>A Reports user should be able to select by [Locations, Pools, Areas], Resource Types, or a single Resource Collection (which is a predefined set of either [more than one Resource Type] or [some combination of more than one Location, Pool, or Area]). |
| 2.2 | Report Filters | When multiple Pools are selected, should sums be disabled or multiple pools disallowed to prevent double-counting? | BRD notes: 'Pools one or more – avoid double counts – disallow multiple pools OR disable sums? Selecting a pool auto-disables location filters.' | Decision needed: disallow multi-pool selection entirely, OR allow but suppress sum columns with a warning? | Directly affects the report DB query and whether GROUP BY logic needs de-duplication. | We want to effectively disallow the selection of any pools that have overlap. E.g., Pool 1 has Area A and Area B, and Pool 2 has Area B and Area C. If Pool 1 is selected and selection of Pool 2 is selected, warn that there is overlap/conflict with Pool 1 (see choices below). Alternatively, adding a Pool could simply ask the user if they wish to add the underlying Areas or are interested in the Pool aggregate counts. Pool for grouped reporting or, if they only care about Area counts, simply add the associated/underlying groups and the Pools function as bulk selectors of Areas.<br><br>**User Options:**<br>1) Do not add Pool 2 and instead create a separate report to avoid double counting<br>2) Disable Pool groupings (deselect pools) and simply include the specific Areas (in this case, keep Areas A & B, add Area C, deselect Pool 1 while keeping Areas A, B, & C)<br>3) Include Pool 2 but disable grand totals |
| 2.3 | Report Filters | What is the 'Sub-Group' filter scope — does filtering by Sub-Group also auto-disable the Location/Area filters? | BRD lists 'Sub-Group' as a standalone filter alongside Locations and Areas. | Confirm cascade: if Sub-Group is chosen, should Location and Area be auto-selected/locked, or remain independently editable? | Without this rule we cannot build the filter dependency graph. | This could probably just be a check box. Display subgroups yes/no. No need to filter down to a single sub-group. |
| 2.4 | Report Filters | What is a 'Resource Group (reporting group)' — how is it different from Resource Collection, and how is it defined? | BRD lists both 'Resource Collection' and 'Resource Group reporting group' as separate filter options in the Reports section. | Clarify whether Resource Group is a new entity or an alias. If new, what fields does it have and how is it managed? | Determines if a new DB table/entity is needed. | I think Resource Group was the original label given to Areas. It differs from a Resource Collection which is an ad hoc grouping of Resources as described a few cells above (G9). |
| 2.5 | Report Filters | Is 'Resource Collection' always available as a filter, or only if at least one collection is defined? | BRD marks 'Resource Collection?' with a question mark as a filter option, implying uncertainty. | Confirm: include or exclude the filter; if included, hide or grey-out when no collections exist? | Needed to handle empty-state UI and conditional filter rendering. | I think it would easier to display it whether a Resource Collection exists or not. If greying out, please ensure that it informs as to the reason (i.e., "No Resource Collections exist" |
| 2.6 | Report Filters | What date/time range filter options are available when generating a report? | BRD references 'Audit – report/audit to see configuration changes in the past X number of hours/days' but gives no standard date filter spec for general reports. | Confirm: Today, Last 7/30/90 days, custom range, specific date? Is a time-zone selector included? | Without this, the report time-range filter cannot be built. | The existing solution allows for selection of any date/time range via date/time selector/picker. |

## 📄 SECTION 3 – Report Types & Content

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems | Reports to Cover New Features | Current Solution (relabeled Computer Group to Area, Computer to Resources, etc.) |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|------------------------------|----------------------------------------------------------------------------------|
| 3.1 | Report Types | What is the full list of report types available in the system? | BRD references a MyPC report list sent separately ('MyPC7ReportsGuide...') but does not reproduce the list inline. | Provide the full report type list with names and descriptions so we can build the report engine and templates. | We cannot build a report module without knowing what reports exist. | The existing solution has a fairly comprehensive list of reports that would be a good starting point.<br><br>**Report Categories:**<br>- **Auditing**<br>- **Reservations** (details on reservations by type (not individual listings)<br>- **Resources** (details on resources)<br>- **Sessions** (details/counts on sessions - include/exclude no-show, canceled, etc.)<br>- **Transactions** (data dump essentially)<br>- **Users & Groups** (information on users and user group details* and history) | User Level Audits<br>Admin Level Audits<br>Upcoming Reservations<br>Reservation Stats*<br>Activity Report**<br><br><br>*Details by Reservation Type with numbers for Personal Time, Late Time, Courtesy Time, Free Time, Wait Time, Remaining Time, etc.<br><br>**Unique Users + Session Count + Minutes Used + Reservation Counts by Type + Counts by Flags + Counts by User Groups / User Types? + Etc… | Canceled Scheduled Reservations<br>Area Hourly Statistics<br>Area Summary<br>Resource Transactions<br>Resource Type Summary<br>Resource Utilization<br>Resource Version Numbers<br>Scheduled Reservations<br>User Computing Detail<br>User Computing Summary<br>Alert List<br>Report List<br>Group Transactions<br>Transactions<br>User Transactions<br>Group Details<br>Group User List<br>User Details<br>User List |
| 3.2 | Report Types | Is there a 'Transaction Report' type, and if so, what fields does it expose? | BRD's Transactions section details many fields (Reservation Type, Duration Type, Free Time Used, Personal Time Used, Late Time Used, Wait Time, Origin, Flags, etc.) but does not confirm a named 'Transaction Report.' | Confirm Transaction Report exists; provide field list and default sort order. | Determines the transaction report schema and output columns. | Pretty much everything? Is there any data that we should *not* include other than the SQL record ID? The current solution uses views so that views can be modified without needing to adjust underlying queries and/or to allow for greater flexibility in modifying the database structure (the view portion can be kept the same and everything is still happy on the reporting side). | | |
| 3.3 | Report Types | Is there an 'Audit Report' that shows configuration changes, and what objects/actions are auditable? | BRD states: 'Audit – report/audit to see configuration changes in the past X number of hours/days.' The System section also mentions Audit Logs. | Confirm whether the Audit Report is a standalone report type or just the Audit Logs viewer. List auditable objects (Users, Resources, Locations, Reservations, System settings, etc.). | Determines if audit is a report type or a separate log viewer — two different engineering paths. | Yes. Realtime data can be observed through a log viewer. More finely tuned and filtered data can be retrieved through a report. | | |
| 3.4 | Report Types | Does the system have a 'Usage / Session Summary Report' (e.g., sessions per day, avg session time)? | BRD's Dashboard widgets show 'Total Reservations,' 'Avg Session Time,' 'Avg Queue Wait,' implying session summary data exists. | Confirm if a dedicated Usage Summary Report exists; specify grouping options (by day, location, resource type, user group). | Needed to design aggregation queries and summary report layout. | Yes.<br><br>**Reporting Range:** Year, Half?, Quarter, Month, Week, Yesterday, Today, Custom<br>**Range Modifiers:** Current (Unit To Date), Current + Previous, Previous<br>**Modifier Quantity:** Integer (e.g., 3 to show data from previous 3 months)<br>   --- N/A if Current (To Date) selected - must include some type of Previous<br>**Filter By:** User Tier*, User Type, User Group, Location, Pool, Area, Resource Type, etc...<br>**Group By Date:** Year, Half?, Quarter, Month, Week, Day of Week, Day of Month, Hour of Day<br>**Group By:** Report specific data/info (Location, Pool, Area, Resource, etc... incl. User Tier*, User Type, User Group)?<br><br>Users can meet the qualifications of multiple User Types, so in the case of a filter, an included User Type means that anyone who fails to match one of the specified User Types would be excluded, even though this may include more data than desired if customer architects poorly.<br><br>In the case of group by we would need to probably disallow grouping by User Types unless you can think of a good way to handle this (e.g., ensuring none of the User Types included in the filter/report possibly have any overlap - which is potentially challenging given the possibility of placeholders). We may dump the burden on the customer to avoid this situation, or perhaps the User Types themselves could have a flag that indicates whether there is any known possible duplicate. | | |
| 3.5 | Report Types | Is there a 'User Activity Report' distinct from the per-user activity view in Users & Accounts? | The Users & Accounts module has 'View User Activity / Last 30 Days / Full History.' Reports module is separate. | Confirm whether Reports module offers a bulk User Activity report filterable by date, location, user group, etc. | Two different features; must be scoped separately in the dev ticket. | Yes. One generates a non-interactive report that can be emailed or exported, and the other presents an on-screen interactive datagrid where staff can click links to view the User's Account, Location, Area, etc. | | |

## 💾 SECTION 4 – Transaction Records & Flags

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems | Additional Notes |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|------------------|
| 4.1 | Transactions | Which transaction flags exist, and what does each flag mean in the DB and in reports? | BRD lists many flags inline: Missed, Cancelled, Extended, Adjusted, Assisted, Group, Abbreviated, Incomplete, Moved, Overdue, Elevated, Prioritized, PushAuth, Rebooted, Technician, TosDeclined, 2FA — plus Kiosk/Portal/Endpoint origin and End User/Staff distinctions. | Confirm the complete official flag list, their DB field names, and whether each is filterable in reports. | Each flag needs a DB column or bitmask; must be confirmed before schema is finalised. | See Transaction details in SignUp Features spreadsheet?<br><br>**RESERVATION TYPES:**<br>Block/Maintenance/Instant/Scheduled/Waitlist/Walk-Up<br><br>**DURATION TYPE:**<br>Full/Quick<br><br>**RESERVATION FLAGS:**<br>**Origin:** Kiosk/Portal/Endpoint<br>**Initiator:** End User/Staff<br>**Labels:** Missed/Cancelled/Extended/Adjusted/Assisted/Group/Abbreviated*/Incomplete/Moved/Overdue/Elevated/Prioritized/PushAuth/Rebooted/Technician/TosDeclined/2FA | |
| 4.2 | Transactions | Transactions for Resource Types or Areas exempt from daily limits — how is the exemption flag stored and how does it interact with reporting? | BRD states: 'Transactions associated with Resource Types or Areas exempt from counting toward daily limits will need a flag to account for them being excluded from a user's daily session limit and/or total minutes allowed.' | Confirm: is this a flag on the transaction record itself, or derived from the Resource Type / Area config at query time? How should exempt transactions appear in reports — included with a label, or excluded? | Directly affects query logic, daily-limit enforcement, and report accuracy. | This has now been answered/detailed in the BRD 2 Queries with Responses and on the Reservations tab of this document. While Free Time pools have been pushed to MVP-2, rather than flagging transactions as free, recorded Transactions will account time in a more dispersed fashion to facilitate higher quality of future reporting and data analytics. | **Time Types:**<br>- Personal Time (time between logon and logoff - *including* any restarts and subsequent time spent logging back in)<br>- Late Time (time between scheduled start and logon - if charged against user)<br>- Courtesy Time (time between scheduled start and logon - not charged against user)<br>- Free Time (session time not charged to Personal Time ... MVP-2)<br>- Wait Time (time between assignment/notification of user and user login)<br>- Remaining Time (guaranteed time left on session when log off/termination happens)*<br><br>*Useful for future enhancements to wait time calculations and/or customer data analytics |
| 4.3 | Transactions | How is 'Free Time Used' recorded in a transaction — as minutes, as a boolean, or as a separate Free Time bucket reference? | BRD states: 'Record Free Time minutes used' and 'Block reservations are always charged to Free Time minutes; Free Time calculations must exclude block reservation transactions.' | Confirm the exact DB field type for Free Time minutes used, and confirm whether the transaction links to a specific Free Time bucket entity. | Needed to design the transactions table and Free Time balance reconciliation logic. | Minutes / integer, and we can discuss whether it reflects the Free Time bucket or simply reflects the Free Time used and the bucket can be retrieved based on the Resource > Area > Free Time association. | |
| 4.4 | Transactions | How is 'Late Time Used' calculated and stored — is it separate from Personal Time Used? | BRD defines 'Late Time Used' as 'used to account for charging missed sessions before timeout and/or late sign-ins for Scheduled sessions.' | Confirm: is Late Time a separate integer field on the transaction, and does it reduce the user's Personal Time balance in the same way as regular session time? | Without this rule we cannot correctly deduct time from user balances. | Let's discuss. L7:014 on the Menus tab touches on this. Maybe some fields can be calculated from the times.<br><br>This has now been answered/detailed in the BRD 2 Queries with Responses and on the Reservations tab of this document. | **Alternative Consideration:**<br>It may make more sense to have a third column for "Special Time" to contain misc. allocations of time for things like Block and Maintenance reservations, auto-start sessions that may or may not count the time against the users, and then the Free Time column can remain pure/isolated.<br><br>**Time Types:**<br>- Personal Time (time between logon and logoff - *including* any restarts and subsequent time spent logging back in)<br>- Late Time (time between scheduled start and logon - if charged against user)<br>- Courtesy Time (time between scheduled start and logon - not charged against user)<br>- Free Time (session time not charged to Personal Time ... MVP-2)<br>- Wait Time (time between assignment/notification of user and user login)<br>- Remaining Time (guaranteed time left on session when log off/termination happens)*<br><br>*Useful for future enhancements to wait time calculations and/or customer data analytics |
| 4.5 | Transactions | What is 'Wait Time' in a transaction — is it the queue wait before assignment, and is it visible in reports? | BRD defines: 'Wait Time = time between creation of reservation and assignment to a PC or cancellation.' | Confirm whether Wait Time is always recorded even for instant walk-ups (would be near-zero), and confirm it is a reportable field. | Needed for wait-time analytics and estimator logic. | Wait time would be zero for a walk up reservation. NULL, 0, or N/A. It is reportable. | |
| 4.6 | Transactions | What does 'Origin of Reservation' mean — and what are the valid origin values? | BRD states: 'Origin of Reservation = Specific Authenticated Resource, Kiosk, or Portal.' | Confirm the complete enumeration of origin values and whether 'Staff Admin Portal' is a separate origin from 'Portal.' | Determines the origin enum in the DB schema. | Origin of Reservation is the endpoint from which a reservation was generated - Kiosk (specific kiosk), Resource (specific resource name), Access Portal (URL) | |
| 4.7 | Transactions | What timestamps are stored per reservation transaction, and are all of them reportable? | BRD lists: Res creation time, Res assignment time, Res scheduled time, Res logon time, Res canceled/missed time. | Confirm all five timestamps are mandatory fields on every transaction record, and which ones carry NULL when not applicable (e.g., canceled before logon). | Needed to finalise the transactions table schema with correct nullable columns. | Agreed. Finalizing the transaction details is needed. | |
| 4.8 | Transactions | Who or what is recorded as the cancelling entity — and is 'Res Cancelling Account' a required field? | BRD lists: 'Res Cancelling Account: User / Staff / System / Automation.' | Confirm: is this a required field on every transaction, or only populated when a cancellation occurs? What is stored for non-cancelled sessions? | Determines nullability and foreign-key design of the cancelling-account column. | For reservations that are not canceled, not required. Otherwise, it should match up to the record id of the user, staff member, or perhaps a hardcoded number representing the system. | |

## ⏰ SECTION 5 – Report Schedules

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|
| 5.1 | Report Schedules | What fields are required to define a Report Schedule (name, frequency, recipients, format, filter set)? | BRD shows 'Report Schedules' as a sub-section of Reports with View, Create, Modify, Remove actions and a Search QuickAI bar, but gives no field list. | Provide the complete field list for a Report Schedule definition. | Without the field list we cannot build the schedule creation form or the DB table. | We should discuss this in a call if G4 does not provide adequate answers. |
| 5.2 | Report Schedules | What output formats can a scheduled report be delivered in — PDF, CSV, Excel, email inline? | BRD Sign-Off Requirements mention 'Report options: ability to save, schedule, etc., and a full list of reports.' No format list is provided. | Confirm supported output formats and delivery methods (email attachment, download link, in-app notification). | Determines report rendering engine requirements (PDF library, CSV exporter, etc.). | Those formats sound agreeable. Options for headers vs raw data only, lusion |
| 5.3 | Report Schedules | Can a Report Schedule be paused/suspended without deleting it, and who can modify a schedule created by another user? | BRD lists Modify and Remove on schedules but does not mention a Pause/Suspend state. | Confirm: is a Pause option needed? Can any admin modify any schedule, or only the creator and users in their Zone? | Affects schedule state machine and Zone-based permission checks. | Pause is a logical addition. Perhaps similar methods could be applied |
| 5.4 | Report Schedules | Are the filter parameters (location, date range, resource type, etc.) saved with the Report Schedule? | BRD does not explicitly link the report filter set to the Report Schedule entity. | Confirm that all active filter parameters at time of schedule creation are persisted and re-applied at each run. | Critical for scheduled report consistency; determines the schedule-to-filter-set relationship in the DB. | Location scoped by default? Resource filter and timeframe configuration should be saved with the schedule (ie. Prior Day/Week/Month, etc.) |

## 💾 SECTION 6 – Save & Export

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|
| 6.1 | Save & Export | Can a report be saved as a named template for re-use, separate from scheduling? | BRD Sign-Off notes 'ability to save, schedule, etc.' and the Reports sub-menu has a 'Create?' action. This suggests saved reports, but the distinction from scheduling is unclear. | Confirm: is 'Save Report' a separate concept from 'Schedule Report'? Can a saved report be run ad-hoc? | Two different DB entities if so; needs separate design. | Canned reports vs. Custom reports? Save a Custom report and then it can be chosen for scheduled? |
| 6.2 | Save & Export | Can a report result grid be exported on-demand (outside of a schedule) — if so, in what formats? | BRD references 'Usage Export' in Users & Accounts. No equivalent is explicitly stated for the Reports module. | Confirm whether the Reports module supports on-demand export of report results (CSV, Excel, PDF). | Determines whether a manual export button is needed on the report results page. | Yes, Report results should be exportable on-demand: Print (?), Save/Export as PDF,CSV, Excel |

## 🔐 SECTION 7 – Permissions & Zones

| # | Sub-Module | Question | What BRD Says | What We Need to Confirm | Concrete Impact if Missing | Response from TRACSystems |
|---|------------|----------|---------------|------------------------|---------------------------|---------------------------|
| 7.1 | Permissions & Zones | Are Reports and Report Schedules Zone-scoped — can a Location Manager only see reports for their Location? | BRD defines Zones as virtual administrative boundaries limiting scope. The Messaging section explicitly applies Zone permissions to message management, but Reports is not explicitly mentioned. | Confirm whether a user's Zone restricts which reports they can view/run/schedule. | Determines whether every report query must include a Zone filter at the API layer. | Yes, Reports should be Zone-scoped |
| 7.2 | Permissions & Zones | Which User Tiers have permission to Create, Modify, Run, and Delete reports vs. only View/Run? | BRD's Tiers section describes permissions controlled at the User Tier level, but no report-specific permission matrix is given. | Provide the permission matrix: which tiers can Create/Modify/Delete report definitions vs. View/Run only. | Required to configure the permissions matrix for the Reports module. | User Tiers can be defined ad hoc and the assignable permissions specific to *reports* would likely cover:<br>View/Run, Modify, Create, Schedule, Delete, Manage My Reports Only, etc. |
